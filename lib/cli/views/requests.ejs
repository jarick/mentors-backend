
import { RestRequest } from './../../../konig.io-rest/lib'

const serialize = (body) => {
  return {
<% for (const field of fields) { %>    <%- field %>: body.<%- field %>,
<% } %>  }
}

const schema = (id) => {
  return <%- JSON.stringify(json, null, 2); %>
}

const auth = () => true

export default RestRequest(auth, schema, serialize)